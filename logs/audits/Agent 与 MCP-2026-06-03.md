---
audit_target: kb/技术/AI/大模型/Agent 与 MCP.md
audit_date: 2026-06-03
verdict: minor
---
# Audit: Agent 与 MCP.md

> 845 行，long-form。审 4 维度：深度 / 章节 / 链接 / 视觉化+frontmatter。

## 1. 深度与具象度 [✓]

- demo 极充分：订机票 Agent、查订单全链路、修 bug 5 轮调用、Spring AI 三种方案 Java 代码、JSON-RPC payload、SQL 查询都给到具体 token 级。
- 反教科书句式很到位："FC 不会真的调函数，只输出意图声明"、"Skill 不具备任何执行能力，纯文本规则注入包"、"每次工具调用 = LLM 被调 2 次"。
- 1.7 三种通道（A 本地直调 / B WebClient / C MCP）对 Java 后端的视角非常贴合用户背景，决策场景 + 三个易混淆点收尾的归纳模式可复用。
- 小瑕疵：1.5（FC vs MCP）、1.6（Agent vs MCP）、1.7（三种通道）三节都用"查订单"同一例子反复演绎，对读者其实强化记忆，但行文上有 ~80 行的概念回环。可接受，不必合并。

## 2. 论述流畅性 + 章节逻辑 [⚠]

- 一级章节连续：1,2,3,4,5,6 无跳号 ✓。子节 1.1-1.8、3.1-3.4、4.1-4.3 也连续 ✓。
- §1 Agent 占 484 行（占全文 57%），把 1.5 FC vs MCP、1.6 Agent 应用 vs MCP、1.7 三种执行通道、1.8 编译时 vs 运行时全部塞进"§1 Agent"下，但这 4 节实质是"FC/MCP/Agent 三者对照"，归属牵强。建议要么 §1 留 1.1-1.4 纯 Agent 概念，把 1.5-1.8 提为独立的 §2「FC、MCP、Agent 三者对照」，要么把 §2 MCP 协议 stub 上移到 1.5 之前形成更顺的"概念 → 协议 → 执行通道"叙事。
- §2 MCP 协议是 stub（已拆分），但紧接着 §3 @Tool 的内部机制（启动扫描 / tools/list / 完整链路）实际是 MCP Server 端实现的深细节，按内容归属应该跟 MCP 协议子文件 boundary 重新对齐——@Tool 留这里 vs 迁到 MCP 协议子文件，需要一个明确选择，目前两边都讲一部分容易让读者迷路。
- §4.3 Skill 在"Claude Code"小节里第一次出现，但顶部"一句话定位"只提 Agent / MCP / 微调三概念，§6 关系全景表却列出 5 大概念（Agent / Prompt / Skill / FC / MCP）——Skill 在文中"凭空出现"的位置欠铺垫。可在"一句话定位"补一句"Skill 是 Agent 框架的能力扩展包"。

## 3. 链接与双向关联语义 [⚠]

- 文件头 + 文件尾各一个 `> 关联:` 块，共 8 条出向链接，覆盖 llm / prompt-rag / mcp-protocol / 微调 / ai-agent-tools / claude-code-architecture。✓
- 反向链接生态健康：23 个文件反向引用本文件（grep 验证），包括 Spring AI、Harness Engineering、MCP 集成实战、四大设计范式、跨语言架构、读书笔记、课程笔记，关系网完整。
- 缺口 1：§4.3 深入讲 Skill，但没出向链到 `Claude-Code/Skills 渐进式披露架构.md` 和 `Claude-Code/Superpowers TDD Skill 工作流拆解.md`——后两者都反向链到本文，单向缺。
- 缺口 2：§1.7 三种执行通道里 Spring AI 代码大量出现，但没链到 `kb/技术/Java/Spring AI.md`（Spring AI.md 已反向链到本文，缺反向补全）。
- 拆分声明清楚：§2 / §5 都明确指向子文件且语义说明充分。

## 4. 视觉化 + Frontmatter 质量 [⚠]

- Frontmatter: `title: "Agent 与 MCP"` + `description: "Agent循环、MCP协议、FC机制、Skill定位、五者关系"` 准确反映正文 ✓。
- 元信息头 `> 最后整理: 2026-05-23 | 来源: ...` 合规 ✓。
- Mermaid 已用 5 处（1.7 节 1 个 flowchart + 3 个 sequenceDiagram，1.8 节 1 个 flowchart），质量高，sequenceDiagram 把"启动 spawn → tools/list → 调用"流程画得很顺。
- 但仍有 7+ 处 ASCII 框图未 Mermaid 化，与 memory feedback「绘图表达偏好」（Mermaid 优先）冲突：
  - §1.2 Agent 核心循环（思考→行动→观察）—— ASCII 框图，应改 mermaid flowchart
  - §1.6 Agent 应用结构（含 LLM 大脑 / ReAct / 工具调用）—— ASCII，应改 mermaid
  - §3.2 工具发现 tools/list 流程 —— ASCII tree，应改 mermaid sequenceDiagram（与 1.7 风格一致）
  - §3.3 完整请求链路 9 步 —— ASCII，应改 mermaid sequenceDiagram
  - §4.1 Claude Code 内部循环 —— ASCII，应改 mermaid flowchart
  - §4.2 修 bug 5 轮调用 —— ASCII 表格框，可考虑 mermaid sequenceDiagram 或保留为表
  - §6 五者关系全景图（最庞大）—— 40+ 行 ASCII，最该 Mermaid 化，否则浏览器 overview.html 无法可视化渲染
- 表格质量高（FC vs MCP / Agent vs MCP / 三方案 / 编译 vs 运行时 / 概念对照 共 5 张），表头清晰，列对齐。

## 行动建议（按优先级）

1. **Important（视觉化）：§6 五者关系全景图 → Mermaid flowchart。** 这张是全文的"压轴归纳图"，浏览器渲染体验影响最大，且用户已通过 memory 明确表达 Mermaid 偏好。
2. **Important（章节归属）：考虑把 §1.5-1.8 提为独立 §2「FC、MCP、Agent 三者对照与执行通道」**，把当前 §2 MCP stub 合并到 §3 @Tool 之前。叙事会更顺，§1 也不会臃肿到 484 行。
3. **Minor（链接补全）：§4.3 末尾追加** `> 关联: [Skills 渐进式披露架构](<../Claude-Code/Skills 渐进式披露架构.md>) | [Superpowers TDD Skill](<../Claude-Code/Superpowers TDD Skill 工作流拆解.md>)`，并在 §1.7 Spring AI 代码段后补一条 `> 关联: [Spring AI](../../Java/Spring AI.md)`。
4. **Minor（铺垫）：「一句话定位」增加一句关于 Skill 的引入**，避免 §4.3 / §6 突现概念。
5. **Minor（视觉化批量）：§1.2、1.6、3.2、3.3、4.1 五处 ASCII 框图分批 Mermaid 化**——优先级低于 §6 但是同质工作可一并做。
6. **Optional（boundary）：明确 @Tool 内部机制（§3）vs MCP 协议子文件的归属。** 当前在两边都讲一部分，建议本文保留"@Tool 是什么 + 一句话总结"，把 3.1-3.3 启动/发现/请求链路全文迁到 MCP 协议子文件，本文只引"详见 MCP 协议：AI 界的 USB-C"——保持 Hub 文件聚焦"概念 + 五者关系"，深细节走子文件。
