# 2026 第37周 (09.07 - 09.13)

## 2026-09-11 周五

- **[技术/计算机基础]** 新增概念笔记《沙箱（Sandbox）：从进程隔离到 Agent 运行时》：把"沙箱"拆成三种语境（业务环境隔离 / Agent 运行时工具约束 / OS 级隔离原语），含三条不变量、OS 隔离谱系、业务链路四维隔离（环境/流量/数据/凭证）、DSH 模式表与"围栏 vs 内核边界"分界、5 问判据、常见误解 → [沙箱（Sandbox）](<../kb/技术/计算机基础/沙箱（Sandbox）：从进程隔离到 Agent 运行时.md>)
- **[技术/AI]** DSH 笔记补 §7「沙箱与权限：两条强制通道」：能力 seam 拆分（策略归属/执行后端/消费方）、逐调用策略、fs 围栏 vs bash 内核 runner、审批升权时序、PermissionPresets、已知限制（原 §7 顺延为 §8） → [DSH 插件架构与循环调度](<../kb/技术/AI/AI-Coding/DSH（DeepSeek Harness）插件架构与循环调度.md>)
- **[基础设施]** 四份只读审计（沙箱笔记 / 规则与元数据 / AI 子树 / Java·实战子树）后执行三批修复：
  - **引链接体系根因**：`build-index.js extractLinks` 现支持 `](<…>)` 与 `[[x.md]]`（反链图覆盖率从 74/382 提升到全量）；新增 `scripts/check-links.js` 统一校验三种链接写法；`check-anchors.js` 补齐同文件 `](#anchor)` 与尖括号跨文件锚点；`app.js` 新增 `convertWikiLinks` 让 14 处 `[[./x.md]]` 关联行在页面可点；`integration.test.js` 增加对应门禁断言（软 TDD 先红后绿）
  - **内容纠错**：AI 子树 / Java 子树 / 大模型·基础·应用 三线并行修正事实错误（Accept-Edit 语义、MCP Server 支持、hook 现状、RocketMQ hold 时长、热点账户状态机、Redis 跳表 p 值、Spring IOC 步骤号、CAP 口径等）、死锚点、过时数字与补互链
  - **规则与元数据收敛**：`AGENTS.md` 对齐 `CLAUDE.md`（arch-lint 15 项、目录树纠错、补「自动沉淀纪律 / Subagent 调度纪律 / Skill 开发纪律（SDD）」三章、Timeline 口径按 ADR-002）；`CLAUDE.md`/`README.md`/`README_EN.md` 的 `%20` 链接清零；`memory/MEMORY.md` 补漏索引；3 个 plan 状态校准；timeline 周记里 26 处拆目录前的旧路径修正 → [决策记录](../docs/decisions.md)
